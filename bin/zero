#!/usr/bin/env bash
set -euo pipefail

root="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
native="$root/.zero/bin/zero"

if [[ ! -x "$native" ]]; then
  echo "zero: native compiler is not built yet" >&2
  echo "run: make -C native/zero-c" >&2
  exit 127
fi

exec "$native" "$@"
