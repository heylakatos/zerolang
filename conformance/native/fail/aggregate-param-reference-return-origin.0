shape Box {
    value: ref<i32>,
}

fun id(box: Box) -> Box {
    return box
}

pub fun main() -> Void {
    let mut x = 1
    let y = 2
    let mut box: Box = Box { value: &x }
    let copy: Box = id(box)
    box = Box { value: &y }
    x = 3
}
