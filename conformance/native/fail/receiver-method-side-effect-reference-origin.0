shape Holder {
    value: ref<i32>,

    fun replace(self: mutref<Self>, next: ref<i32>) -> Void {
        self.value = next
    }
}

pub fun main() -> Void {
    let mut x = 1
    let mut y = 2
    let mut holder = Holder { value: &y }
    holder.replace(&x)
    x = 3
}
