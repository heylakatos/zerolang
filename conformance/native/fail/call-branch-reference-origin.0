fun choose(flag: Bool, left: ref<i32>, right: ref<i32>) -> ref<i32> {
    let mut current = left
    if flag {
        current = right
    }
    return current
}

pub fun main(flag: Bool) -> Void {
    let mut x = 1
    let y = 2
    let current = choose(flag, &x, &y)
    x = 3
}
