pub fun main() -> Maybe<Void> raises {
    let mut x = 1
    let y = 2
    let mut refs = [&x]
    let maybe = std.mem.get(refs, 0)
    let current = check maybe
    refs = [&y]
    x = 2
    return null
}
