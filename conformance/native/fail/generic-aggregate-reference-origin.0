shape Box {
    value: ref<i32>,
}

fun id<T>(value: T) -> T {
    return value
}

pub fun main() -> Void {
    let mut x = 1
    let y = 2
    let mut box: Box = Box { value: &x }
    let copy: Box = id(box)
    box = Box { value: &y }
    x = 3
}
