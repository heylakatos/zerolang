shape Holder {
    value: ref<i32>,
}

fun storeLocal(holder: mutref<Holder>) -> Void {
    let local = 1
    holder.value = &local
}

pub fun main() -> Void {
    let fallback = 0
    let mut holder = Holder { value: &fallback }
    storeLocal(&mut holder)
}
