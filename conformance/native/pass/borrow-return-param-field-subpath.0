shape Point {
    x: i32,
    y: i32,
}

fun field(point: ref<Point>) -> ref<i32> {
    return &point.x
}

pub fun main() -> Void {
    let mut point = Point { x: 1, y: 2 }
    let current = field(&point)
    point.y = 3
}
